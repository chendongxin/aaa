<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <contextName>mustang-log</contextName>


    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springboot.sample" level="TRACE"/>

    <!-- 开发、测试环境 -->
    <springProfile name="dev,dev2,test">
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="org.springboot.sample" level="INFO"/>
        <logger name="com.hq" level="DEBUG"/>
    </springProfile>

    <!-- 生产环境 -->
    <springProfile name="pro">
        <logger name="org.springframework.web" level="ERROR"/>
        <logger name="org.springboot.sample" level="ERROR"/>
        <logger name="com.hq.mustang.modules.allot" level="DEBUG"/>
        <logger name="com.hq.mustang.modules.sys.dao" level="INFO"/>
        <logger name="com.hq" level="INFO"/>
    </springProfile>

    <appender name="consoleLog" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </layout>
    </appender>

    <appender name="fileInfoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--路径-->
            <fileNamePattern>logs/%d.mustang.log</fileNamePattern>
        </rollingPolicy>
    </appender>

    <!-- 日志文件记录 -->
    <root level="INFO">
        <!--<appender-ref ref="consoleLog"/>-->
        <appender-ref ref="fileInfoLog"/>
    </root>

</configuration>